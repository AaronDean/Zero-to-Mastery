{
  "name": "lecture_0200",
  "version": "1.0.0",
  "description": "first package.json file for ZtM course.",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify script.js > bundle.js && live-server"
  },
  "author": "Aaron Dean",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.20"
  }
}
